// regular function

function addOne(num: number) {
  return num + 1;
}

const res = addOne(5);
console.log(res);

// arrow function
let multiply = (x: number, y: number) => x * y;

let result = multiply(2, 5);
console.log(result);
